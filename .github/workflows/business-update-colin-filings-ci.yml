name: Business Update Colin Filings Job CI

on:
  pull_request:
    types: [assigned, synchronize]
    paths:
      - "jobs/update_colin_filings/**"
  workflow_dispatch:

defaults:
  run:
    shell: bash
    working-directory: ./jobs/update_colin_filings

jobs:
  business-update-colin-filings-ci:
    uses: bcgov/bcregistry-sre/.github/workflows/backend-ci.yaml@main
    with:
      app_name: "business-future-effective-filings"
      working_directory: "./jobs/update_colin_filings"
      codecov_flag: "updatecolin"
