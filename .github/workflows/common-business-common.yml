name: Common Business Common CI

on:
  pull_request:
    types: [assigned, synchronize]
    paths:
      - "python/common/business-registry-common/**"
  workflow_dispatch:

defaults:
  run:
    shell: bash
    working-directory: ./python/common/business-registry-common

jobs:
  business-registry-common-ci:
    uses: bcgov/bcregistry-sre/.github/workflows/backend-ci.yaml@main
    with:
      app_name: "business-registry-common"
      working_directory: "./python/common/business-registry-common"
      codecov_flag: "business-registry-common"